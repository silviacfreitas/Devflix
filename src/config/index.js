/* eslint-disable linebreak-style */
const URL_DATA = window.location.hostname.includes('localhost')
  ? 'http://localhost:3030'
  : 'https://tripflix-project.herokuapp.com';

export default {
  URL_DATA,
};
